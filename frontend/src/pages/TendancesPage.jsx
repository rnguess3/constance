// Écran "Tendances" — pas encore construit (hors scope de cette tâche).
// Sert de destination valide à l'onglet de navigation basse.
export default function TendancesPage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-2 px-4 pb-24">
      <h1 className="font-display text-2xl font-semibold text-teal">Tendances</h1>
      <p className="font-sans text-sm text-neutral-500">Bientôt disponible.</p>
    </main>
  );
}
